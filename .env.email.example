# Configuración de Email para Gatekeeper
# =====================================

# Configuración principal de Mail
MAIL_MAILER=failover
MAIL_FROM_ADDRESS=your-email@yourdomain.com
MAIL_FROM_NAME="Gatekeeper System"

# SendGrid Configuration (Método principal)
# Obtén tu API key desde: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here

# SMTP Configuration (Fallback)
# Puedes usar Gmail, Outlook, o cualquier servidor SMTP
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_TIMEOUT=20

# Queue Configuration
# Recomendado: usar 'database' para producción
QUEUE_CONNECTION=database

# =====================================
# Instrucciones de configuración:
# =====================================

# 1. SENDGRID (Recomendado como método principal):
#    - Crea una cuenta en SendGrid
#    - Genera un API Key
#    - Reemplaza 'your_sendgrid_api_key_here' con tu API key real
#    - Verifica tu dominio en SendGrid

# 2. GMAIL SMTP (Como fallback):
#    - Habilita 2FA en tu cuenta de Gmail
#    - Genera una "App Password"
#    - Usa esa App Password en MAIL_PASSWORD
#    - NO uses tu contraseña normal de Gmail

# 3. OTROS SMTP (Alternativas):
#    # Outlook/Hotmail:
#    MAIL_HOST=smtp-mail.outlook.com
#    MAIL_PORT=587
#
#    # Yahoo:
#    MAIL_HOST=smtp.mail.yahoo.com
#    MAIL_PORT=587

# 4. CONFIGURACIÓN DE QUEUE:
#    - Ejecuta: php artisan migrate (para crear tabla jobs)
#    - Ejecuta: php artisan queue:work --queue=notifications,default
#
# =====================================
